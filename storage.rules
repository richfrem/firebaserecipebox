rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow writes to the 'recipes' folder for authenticated users
    match /recipes/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
